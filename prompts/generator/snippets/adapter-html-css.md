【输出目标：HTML/CSS】
- 输出一个“可直接打开运行”的单文件 HTML
- 必须包含：完整 <head>（title/description/viewport/meta charset）、内嵌 <style>
- 可选 JavaScript（仅用于必要交互，例如移动端菜单、tabs、dropdown、轻量滚动进入）
- CSS 必须使用 CSS 变量定义颜色/间距/字体，避免散落魔法值
- 语义化 HTML：header/nav/main/section/footer

【输出格式要求】
- 先用 6–10 条要点复述你将执行的“DNA + 禁止清单”
- 然后输出完整 HTML（从 <!DOCTYPE html> 开始）


